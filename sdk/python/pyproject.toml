[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "autopilon-sdk"
version = "1.0.0"
description = "Minimal Python client for the Autopilon Public API (v1)."
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
include = ["autopilon*"]
